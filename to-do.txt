More Important:
On initialization, Tile class must determine all unique orientations of itself
Board, given a TileBag, must return a List<Move> of all possible moves.
TileBag needs a print function.
Add CLI (UserInput) so player can input their own moves.
Add Minimax ADT and Node class so CPU can find best moves.
Implement GameManager to iterate through all of the players.
Create GUI in Display class.

Less Important:
In TileBag, only initialize one TileBag, then create deep copies for the rest
Make TileBag implement Iterable